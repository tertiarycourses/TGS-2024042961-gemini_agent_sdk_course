# WSQ assessment set is CONFIDENTIAL — Google Drive / LMS only, never GitHub
assessment/
reference/
reference/tms/

# Secrets — never commit
.env
*.env
!.env.example
**/.env

# Build/tooling
.claude/
.adk/
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Vector store artifacts (regenerated by the labs)
**/chroma_db/

# Archives
*.zip

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Superseded courseware versions — kept locally, never committed
courseware/archive/
